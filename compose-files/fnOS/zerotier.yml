services:
  zerotier:
    image: zerotier/zerotier:latest
    container_name: zerotier
    privileged: true
    volumes:
      - /vol1/1000/Docker/zerotier:/var/lib/zerotier-one
    restart: always
    network_mode: host
