services:
  verysync:
    image: jonnyan404/verysync:latest
    container_name: verysync
    ports:
      - 26010:8886
    volumes:
      - /volume1/@home/Testroot/Synchronous:/data
    restart: always
    network_mode: bridge
